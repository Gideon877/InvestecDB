// SELECT relationships.Parent_Entity_Name FROM relationships INNER JOIN limits ON relationships.Entity_Id = limits.Entity_Id;
// SELECT COUNT(*) AS children, Parent_Entity_Name FROM relationships GROUP BY Parent_Entity_Name ;
